package com.example.gps_test4.model;

// 동·읍·리를 담을 Class
public class Dong extends LocationData {
    // LocationData 클래스를 상속받기 때문에 추가적인 기능이 필요가 없다.
    // Dong 클래스에는 ArrayList 가 필요하지 않다.
    // 즉, LocationData (부모)클래스에 있는 멤버 변수 String code, location_name 만 있어도 무관하다.
}